useTabs: true
singleQuote: false
trailingComma: all
printWidth: 140
quoteProps: as-needed

# Add from Svelte
plugins:
  - prettier-plugin-svelte
overrides:
  - files: "*.svelte"
    options:
      parser: svelte
